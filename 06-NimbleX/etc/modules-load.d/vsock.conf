# Load the host-side virtio vsock backend at boot so this machine can serve
# SSH-over-AF_VSOCK to the VMs/containers it runs (e.g. the NimbleX test VM
# reached via "ssh root@vsock/<cid>"). Harmless if unused; ~24K module.
vhost_vsock
